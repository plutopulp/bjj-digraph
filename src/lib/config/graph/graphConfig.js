import { nodeTypes } from "../types/nodeTypes";
import { nodeSubtypes } from "../types/nodeSubtypes";
import { edgeTypes } from "../types/edgeTypes";

export const graphConfig = {
  nodeTypes,
  nodeSubtypes,
  edgeTypes,
};

export const NODE_KEY = "id";
